services:
  ux-watcher:
    build:
      context: ../..
      dockerfile: deploy/ux-watcher/Dockerfile
    container_name: ux-watcher
    restart: unless-stopped
    environment:
      TZ: Europe/Berlin
    volumes:
      - /opt/secrets/google-sa.json:/secrets/google-sa.json:ro
      - /opt/secrets/ux-watcher.env:/secrets/ux-watcher.env:ro
